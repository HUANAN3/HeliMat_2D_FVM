{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            //"label": "CFD_Run",
            "command": "D:/SOFT/mingw64/bin/g++.exe",
            "args": [
                // "-g",
                "-std=c++20",
                "-fopenmp",             // 启用 OpenMP
                "-O3",                  // 最高优化级别
                "-march=native",        // 为本地 CPU 架构优化
                "-I${workspaceFolder}/include",
                "${workspaceFolder}/src/*.cpp",
                "-o",
                "${workspaceFolder}/bin/${fileBasenameNoExtension}.exe"
                // "-std=c++20",
                // "-fopenmp", 
                // "-O3", 
                // "-march=raptorlake",
                // "-mtune=raptorlake", 
                // "-static",
                // "-I${workspaceFolder}/include",
                // "${workspaceFolder}/src/*.cpp",
                // "-o",
                // "${workspaceFolder}/bin/Riemann2D.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
            
        }
    ],
    "version": "2.0.0"
}